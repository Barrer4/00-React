//función que devuelve el factorial de un número que le llega por argumento. 

function factorial(n) {
   let number = 1
   for (let i = 0; i < i<= n; i++) {
      number = number * i
   }
   return number
}